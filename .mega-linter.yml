
# https://megalinter.io/latest/supported-linters/
ADDITIONAL_EXCLUDED_DIRECTORIES:
    - vendor
FILTER_REGEX_EXCLUDE: (vendor/)
ENABLE_LINTERS:
    - ACTION_ACTIONLINT
    - ANSIBLE_ANSIBLE_LINT
    - BASH_SHELLCHECK
    - DOCKERFILE_HADOLINT
    - GO_GOLANGCI_LINT
    #- MARKDOWN_MARKDOWNLINT
    - PYTHON_RUFF
    #- PROTOBUF_PROTOLINT
    - REPOSITORY_GITLEAKS
    #- REPOSITORY_KICS
    #- YAML_YAMLLINT
MARKDOWN_MARKDOWNLINT_FILTER_REGEX_EXCLUDE: '(^|/)(specs|\.specify)/'
REPOSITORY_KICS_ARGUMENTS: "--fail-on high"
