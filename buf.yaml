version: v2
modules:
    - path: api/plugin
lint:
    use:
        - STANDARD
    except:
        - PACKAGE_DIRECTORY_MATCH
        - SERVICE_SUFFIX
        - ENUM_ZERO_VALUE_SUFFIX
breaking:
    use:
        - FILE
