# Implementation Plan: AMPEL Branch Protection Scanning Plugin

**Branch**: `001-ampel-branch-scan` | **Date**: 2026-02-11
**Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/001-ampel-branch-scan/spec.md`

## Summary

Build a complyctl plugin that matches OSCAL assessment rules
against granular AMPEL policy files and merges them into a
combined bundle (Generate), then invokes the AMPEL toolchain
(snappy, ampel) to scan branch protection rules on GitHub
repositories (GetResults). The plugin uses only existing project
dependencies, isolates the OSCAL-to-AMPEL conversion layer for
future Gemara migration, handles DSSE-wrapped attestation output,
and groups multi-target results into proper OSCAL observations.

## Technical Context

**Language/Version**: Go 1.24.x (matching parent go.mod)
**Primary Dependencies**:
- `compliance-to-policy-go/v2` (v2.0.0-rc.1) - plugin framework
- `hashicorp/go-plugin` (v1.7.0) - gRPC plugin hosting
- `hashicorp/go-hclog` (v1.6.3) - structured logging
- `goccy/go-yaml` (v1.19.2) - YAML parsing for target config
- `stretchr/testify` (v1.11.1) - test assertions
- `encoding/json` (stdlib) - AMPEL policy JSON I/O
- `os/exec` (stdlib) - external tool invocation
**Storage**: Filesystem (workspace directory)
**Testing**: `go test -race -v ./...` with testify assertions
**Target Platform**: Linux (primary), macOS (development)
**Project Type**: Single binary (complyctl plugin subprocess)
**Constraints**: Zero new dependencies; reuse only libraries
already in go.mod
**Scale/Scope**: 10-100 repositories per scan run

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-checked post-design.*

| Principle | Status | Evidence |
|-----------|--------|----------|
| I. Plugin Contract Compliance | PASS | Implements policy.Provider (Generate, GetResults, Configure) via compliance-to-policy-go framework. Manifest at c2p-ampel-manifest.json. |
| II. Security-First | PASS | Input validation on config, target URLs, and tool output. No secrets in logs. External tool output sanitized before returning. |
| III. Test-Required | PASS | Unit tests for all packages with testdata fixtures. Integration test for Generate→GetResults round-trip. Race detection enabled. Coverage tracked. |
| IV. Observability | PASS | hclog JSON logging to stderr for all operations. Errors include operation context and identifiers. Sensitive data excluded from logs. |
| V. Simplicity | PASS | Zero new dependencies. Granular packages with single responsibilities. No speculative features. Isolated conversion layer (not an over-abstraction — justified by explicit Gemara migration plan). |

**Post-design re-check**: All gates pass. No violations to track
in Complexity Tracking.

## Project Structure

### Documentation (this feature)

```text
specs/001-ampel-branch-scan/
├── spec.md
├── plan.md              # This file
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── target-config-schema.yaml
│   └── plugin-manifest-schema.md
├── tasks.md             # Generated by /speckit.tasks
└── checklists/
    └── requirements.md
```

### Source Code (repository root)

```text
cmd/ampel-plugin/
├── main.go                        # Entry point, plugin registration
├── config/
│   ├── config.go                  # Configuration parsing + validation
│   └── config_test.go
├── server/
│   ├── server.go                  # policy.Provider implementation
│   └── server_test.go
├── convert/
│   ├── convert.go                 # Granular policy matching + merging
│   ├── types.go                   # AMPEL policy data structures
│   ├── convert_test.go
│   └── testdata/
│       ├── assessment-plan-full.json
│       ├── assessment-plan-subset.json
│       ├── ampel-policy-expected-full.json
│       ├── ampel-policy-expected-subset.json
│       └── ampel-policy-existing-broader.json
├── toolcheck/
│   ├── toolcheck.go               # System tool presence validation
│   └── toolcheck_test.go
├── targets/
│   ├── targets.go                 # Target config file parsing
│   ├── targets_test.go
│   └── testdata/
│       ├── valid-targets.yaml
│       ├── duplicates-targets.yaml
│       ├── empty-targets.yaml
│       └── invalid-url-targets.yaml
├── scan/
│   ├── scan.go                    # External tool invocation orchestration
│   ├── scan_test.go
│   └── specs/                     # Embedded snappy spec files
│       └── github/
│           └── branch-rules.yaml
├── results/
│   ├── results.go                 # AMPEL output → PVPResult + per-repo files
│   ├── results_test.go
│   └── testdata/
│       ├── ampel-verify-pass.json
│       ├── ampel-verify-fail.json
│       ├── ampel-verify-dsse-fail.json  # DSSE-wrapped attestation fixture
│       └── ampel-verify-error.json
├── docs/
│   ├── STRATEGY.md                # Plugin strategy document
│   └── samples/
│       ├── c2p-ampel-manifest.json
│       └── ampel-targets.yaml
└── README.md                      # Plugin documentation
```

**Structure Decision**: Single binary plugin following the
openscap-plugin layout. Seven packages (config, server, convert,
toolcheck, targets, scan, results) provide granular separation
per the user's requirement. Each package has a single, clearly
defined responsibility.

### Package Responsibilities

| Package | Responsibility | Key Dependency |
|---------|---------------|----------------|
| `main` | Plugin registration with hashicorp/go-plugin | plugin framework |
| `config` | Parse manifest config map, validate paths, create dirs | stdlib + hclog |
| `server` | Implement policy.Provider, orchestrate other packages | policy framework |
| `convert` | Match OSCAL rules to granular AMPEL policies, merge into bundle (isolated for Gemara migration) | policy types |
| `toolcheck` | Validate snappy, ampel are on PATH | os/exec |
| `targets` | Parse ampel-targets.yaml, validate, deduplicate | goccy/go-yaml |
| `scan` | Invoke snappy + ampel verify per repo, capture output | os/exec |
| `results` | Parse ampel verify output → PVPResult, write per-repo JSON | encoding/json |

### API Isolation for Gemara Migration

The `convert` package is the only package that understands OSCAL
data structures. All other packages work with AMPEL types or
generic plugin types. When complyctl migrates from OSCAL to
Gemara:

1. Only `convert/convert.go` changes (the matching function)
2. `MatchPolicies` evolves from accepting `[]extensions.RuleSet`
   to accepting Gemara policy identifiers
3. `LoadGranularPolicies` and `MergeToBundle` remain unchanged
4. All downstream packages (scan, results, targets, config)
   remain untouched

### Test Architecture

**Unit tests per package** cover:
- Happy path with valid inputs
- Unexpected/invalid inputs (empty, nil, malformed)
- Boundary conditions (empty policy, empty target list)
- Error propagation

**Mock fixtures** in `convert/testdata/`:
- Mocked OSCAL assessment plan (full and subset variants)
- Expected AMPEL policy output for each plan variant
- Pre-existing broader AMPEL policy for scope-honoring tests

**Verification of assessment plan ↔ AMPEL policy linkage**:
- Table-driven tests compare generated policy against expected
  fixtures
- Each test case modifies the input assessment plan and verifies
  the output changes accordingly
- Tests assert exact tenet count, tenet IDs, and CEL expression
  content

## Complexity Tracking

No constitution violations. No complexity justification needed.
